export type MapData = {
  id: string;
  title: string;
  color: string;
  svgPath?: string;
  latitude?: number;
  longitude?: number;
};

export type MapLegend = {
  title: string;
  color: string;
};
